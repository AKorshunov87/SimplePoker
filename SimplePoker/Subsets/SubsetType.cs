namespace SimplePoker.Subsets {
    /// <summary>
    /// Subset types ( Flush, Four of a Kind, etc)
    /// </summary>
    internal enum SubsetType {
        HighCard = 1,
        TwoOfAKind = 2,
        ThreeOfAKind = 3,
        Flush = 4,
        FourOfAKind = 5
    }
}
